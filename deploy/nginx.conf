server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  location /api/ { proxy_pass http://api:3000; proxy_http_version 1.1; proxy_set_header Host $host; }
  location = /health { proxy_pass http://api:3000/health; }
  location / { try_files $uri $uri.html $uri/ /index.html; }
}
